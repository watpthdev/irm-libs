export enum EParticipantStatus {
  WAITING = "waiting",
  ELIGIBLE = "eligible",
  ELIGIBLE_RESERVE = "eligible_reserve",
  NOT_ELIGIBLE = "not_eligible",
  CANCELED = "canceled",
  ABSENT = "absent",
}
